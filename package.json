{
  "name": "meet-upper-cli",
  "version": "2.0.2",
  "description": "Search events on Meetup from CLI",
  "author": "Jakub Biesiada",
  "license": "MIT",
  "main": "bin/index.js",
  "scripts": {
    "build": "rimraf bin/* && tsc -p .",
    "prettier": "prettier --write 'src/**/*.ts'"
  },
  "repository": "https://github.com/JB1905/meet-upper-cli.git",
  "engines": {
    "node": ">=10"
  },
  "bin": "bin/index.js",
  "dependencies": {
    "chalk": "^3.0.0",
    "commander": "^4.1.0",
    "figlet": "^1.2.4",
    "node-fetch": "^2.6.0"
  },
  "devDependencies": {
    "@types/figlet": "^1.2.0",
    "@types/node": "^13.1.4",
    "@types/node-fetch": "^2.5.4",
    "husky": "^4.0.1",
    "lint-staged": "^9.5.0",
    "prettier": "^1.19.1",
    "typescript": "^3.7.4"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "src/**/*.ts": [
      "npm run prettier",
      "git add"
    ]
  }
}
